# G-1 Security and Supply-Chain Results

Submission target: `58d926778dde76724467b2eab307e80b0a1c5ea3`
Result: PASS
Refresh: exact local/UTC timestamps, commands, cwd, exit status, output digest,
and structured summaries are persisted in `scanner-refresh.json`; all runs cover
submission revision `58d926778dde76724467b2eab307e80b0a1c5ea3`.

The runnable scanners are no longer missing. Current read-only checks are clean
for the scopes stated below. The repository-wide source mypy gate now passes.
AC-GLOBAL-04 now passes because the actual `flow-memory==0.1.1` runtime
dependency is published, pinned in `pyproject.toml`, resolved from official
PyPI on Python 3.10, audited, and verified by a no-cache clean install.

## Current checks

| Check | Tool / command | Exit | Result |
|---|---|---:|---|
| Ruff full repository | `uvx --offline ruff==0.15.10 check src tests scripts acceptance` | 0 | PASS - `All checks passed!`; repository baseline is reduced from 486 to zero |
| Ruff production source | `ruff==0.15.10 check src` | 0 | PASS - zero findings after R2f; no global or per-file ignore was added |
| Full source type check | `mypy 2.2.0`; `UV_NO_PROJECT=1 uvx --offline --from mypy==2.2.0 mypy --ignore-missing-imports src` | 0 | PASS - `Success: no issues found in 155 source files` |
| Git secret scan | `trufflehog 3.95.9 git file://<candidate> --json --no-update --fail --fail-on-scan-errors --results verified,unverified,unknown` | 0 | PASS - 9426 chunks and 18157058 bytes; `verified_secrets=0`, `unverified_secrets=0`, scan error logs 0 |
| Python base dependencies, minimum Python | `pip-audit 2.10.1 --strict --format json .` under Python 3.10 | 0 | PASS - 6 resolved dependencies including `flow-memory==0.1.1`; 0 known vulnerabilities |
| Published dependency clean install | Python 3.10 `pip install --no-cache-dir --index-url https://pypi.org/simple .`; `pip check`; compatibility smoke | 0 | PASS - installed `flow-memory==0.1.1` from PyPI, no broken requirements, legacy vector exports present |
| Python optional vector dependencies | `pip-audit 2.10.1 --strict -r /tmp/eduflow-vector-requirements.XXXXXX.txt` under Python 3.10; exact input is `lancedb>=0.4` plus `sentence-transformers>=2.2` | 0 | PASS - 51 resolved dependencies; 0 known vulnerabilities |
| Node production dependencies | `npm audit --omit=dev --audit-level=high --registry=https://registry.npmjs.org --json` | 0 | PASS - 0 vulnerabilities (0 info/low/moderate/high/critical) across 3 production dependencies and 1 optional entry |
| Node lockfile | exact-pin and complete-lock acceptance tests plus disposable `npm ci --ignore-scripts` | 0 | PASS - Playwright `1.59.1`, lockfile v3, HTTPS resolution and SHA-512 integrity metadata |

## Secret-scanner provenance

The TruffleHog archive and checksum manifest came from the official
`trufflesecurity/trufflehog` GitHub release `v3.95.9`. The darwin-arm64 archive
SHA-256 matched the published value:

`944c6ea3a2993a9f808d08107b40e03ba92bc75972876a1ee47d567bfd6fa1b5`

An earlier filesystem-mode attempt also found zero results but logged four
decode errors against generated `__pycache__/*.pyc` files. It is not accepted
as evidence. The recorded Git-source run scanned committed content/history,
reported zero error logs, and is the authoritative secret-scan result.

`scanner-refresh.json` is the machine-readable provenance record. It binds each
accepted run to an exact command, working directory, local and UTC time, exit
code, output SHA-256/size, and complete result summary. The raw temporary output
files are not treated as durable evidence; their digests protect the recorded
summaries from accidental substitution, while the commands and exact optional
input make each run reproducible.

## Type-checker boundary

The import-following source run covers all 155 modules under `src` and is clean.
It uses `--ignore-missing-imports` only for external/untyped packages; it does
not skip local imports or narrow the file list. The two behavior findings found
during review (unpin exit status and the missing T-104 archive API) were fixed
with explicit regression tests rather than hidden behind type suppression.

## Dependency boundary

The Python audit is attributable to declared EduFlow dependencies, rather than
the shared Homebrew environment. The base project resolves six dependencies on
Python 3.10, including the exact `flow-memory==0.1.1` pin and conditional
`tomli`. The optional vector audit separately covers the currently declared
extra. The clean-install proof resolves from official PyPI without any editable
sibling checkout. PyPI reports wheel SHA-256
`e923681030dd56d6c61ac1741887918ef10a45d6a4717e4fa71893995bde29d3`
and sdist SHA-256
`f06428c0aef1afdc58d789e23e881ff7f725cb17fca6aa4ced6cb65f684e46f5`.

The current official npm registry result supersedes the historical offline
mirror audit for freshness. The lockfile still contains resolved artifact URLs
from `registry.npmmirror.com`; changing those artifact sources requires an
explicit dependency/lockfile remediation rather than relabeling the official
advisory query.

Result: PASS - secret scanning, pip-audit, npm audit, Ruff, full-source mypy,
and the pinned published Flow Memory dependency are current and clean for their
stated scopes. AC-GLOBAL-04 passes.
