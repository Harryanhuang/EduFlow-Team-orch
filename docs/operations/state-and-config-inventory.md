# State and configuration inventory

This is the operator contract for durable EduFlow state, configuration, assets, and credentials. In the table, `$STATE` means the resolved `EDUFLOW_STATE_DIR`.

## Path authority and known drift

The Python authority is `src/eduflow/runtime/paths.py`: `EDUFLOW_STATE_DIR` wins and bare Python falls back to `~/.eduflow`. The repository wrapper and production launch convention set `EDUFLOW_STATE_DIR=$PROJECT_ROOT/.eduflow-team-state`. Therefore `.eduflow-team-state` and `~/.eduflow` are not aliases. Running `python -m eduflow.cli` without the wrapper/environment can silently create or read a second state tree. Recovery and migration must identify the live process environment before copying either tree.

Unified configuration resolves through `EDUFLOW_CONFIG_FILE`, otherwise `./eduflow.toml`. During migration, `src/eduflow/runtime/config.py` still supports legacy/fallback `EDUFLOW_TEAM_FILE` or `./team.json` and `EDUFLOW_RUNTIME_CONFIG` or `./runtime_config.json`. TOML is authoritative when its relevant section exists; legacy JSON is fallback-only and must not be edited as if it overrides TOML.

## Inventory

| Asset | Authoritative location/store | Writer | Reader | Owner | Permissions | Backup | Retention | Recovery | Migration requirement |
|---|---|---|---|---|---|---|---|---|---|
| inbox | `$STATE/facts/inbox.json` | `eduflow.store.local_facts.append_message/mark_read` | inbox, router, residency and status projections | runtime operator | `$STATE` and `facts/` 0700; file private to service account | consistent state-tree snapshot | retain unread plus read audit until the deployment retention window expires | stop writers, restore snapshot, validate JSON, then restart router | copy with the chosen state tree; never merge rows by content; preserve `local_id` |
| task snapshot | `$STATE/tasks.json` | `eduflow.store.tasks` mutations | task CLI, publisher, memory capsule refresh and read models | manager/task control plane | private state tree; atomic locked JSON writes | consistent state-tree snapshot | active tasks indefinitely; archive terminal tasks per operator policy | restore with task events from the same checkpoint and validate `_meta.last_id` | migrate snapshot and task events together; preserve task IDs and versions |
| task events | `$STATE/task-events.jsonl` | `eduflow.store.tasks._append_task_event` | task event scanner, publisher and audit tools | manager/task control plane | private state tree; append-only service-account file | snapshot with `tasks.json` | append-only audit; compact only through an approved archival procedure | restore, tolerate only a truncated final JSONL line, reconcile against snapshot | preserve ordering, event IDs and correlation IDs; do not regenerate history from snapshot |
| generic events | `$STATE/facts/logs.jsonl` | `eduflow.store.local_facts.append_log` | status/workspace/recall projections and operators | runtime operator | private state tree; append-only service-account file | state-tree snapshot or rotated archive | operational audit window set by operator; rotate, do not edit in place | restore latest archive and tolerate only a truncated final JSONL line | preserve timestamps and agent attribution; document any retention cutover |
| router cursor | `$STATE/router.cursor` | Feishu catch-up/router | router catch-up and health | runtime operator | private state tree; single service-account writer | state-tree snapshot | retain current checkpoint while deployment exists | restore with seen set; if unavailable replay from a deliberately chosen safe boundary | migrate with router seen set and inbox to avoid gaps or duplicates |
| router seen set | `$STATE/router.seen` | Feishu router | Feishu router deduplication | runtime operator | private state tree; single service-account writer | state-tree snapshot | bounded by router implementation; retain across restart | restore with cursor; rebuild only by controlled replay | migrate atomically with cursor and inbox; never deduplicate by message text |
| runtime status | `$STATE/facts/runtime-status.json` | runtime lifecycle/switch commands | runtime list, verify, health and lifecycle | runtime operator | private state tree; atomic JSON writes | state-tree snapshot | current declaration plus implementation-kept history | restore, then prove live runtime with verification rather than trusting cache | migrate declarations but re-run live verification after cutover |
| runtime switch events | `$STATE/facts/runtime-switch-events.jsonl` | runtime switch, failover and watchdog via `record_switch_event` | runtime events, audit and diagnostics | runtime operator | private state tree; append-only service-account file | state-tree snapshot or audit archive | append-only operational audit window | restore JSONL and compare final event with live pane/runtime verification | preserve ordering and outcomes; do not treat cached `proved_ready` as live proof |
| loop runs | `$STATE/loop-runs.json` and `$STATE/loop_runs/<L-ID>/` | `eduflow.store.loop_runs` and loop executors | loop CLI, task evidence and operators | workflow owner | private state tree; locked index and private artifacts | snapshot index and artifact directory together | retain evidence at least as long as referenced task/audit | restore index and matching directories; validate every evidence ref | migrate index and artifacts atomically; preserve L-IDs and relative refs |
| workflow assets | repository `docs/workflows/<id>/` | reviewed repository changes | asset registry, workflow CLI, agents and operators | workflow owner | repository ACL; reviewed version control | git remote/release artifact | full git history and supported releases | checkout the approved commit and run strict workflow validation | version with code; no state-tree copy; review local uncommitted assets separately |
| skill assets | repository `skills/<id>/SKILL.md` and `.claude/skills/` | reviewed repository changes | asset registry and agent bootstrap | skill owner | repository ACL; reviewed version control | git remote/release artifact | full git history and supported releases | checkout approved commit and validate skill discovery/frontmatter | version with code; reconcile duplicate legacy `.claude/skills` surfaces explicitly |
| identity assets | `$STATE/agents/<agent>/identity.md` (wrapper production: `.eduflow-team-state/agents/...`) | init/hire/provisioning and authorized role maintainers | reidentify/bootstrap and asset registry | role owner | `$STATE/agents` private; identity readable by that deployment's agents | state-tree snapshot plus reviewed source where available | retain for agent lifetime and audit-relevant replacements | restore the exact role identity before reidentify; verify linked skill/spec exists | treat as deployment state, not the repository default; review drift before copying |
| memory database | `$STATE/eduflow_memory.db` (SQLite WAL) | `eduflow.memory.*` through `src/eduflow/memory/db.py` | memory packet, constraints, capsules, profile and recall surfaces | memory subsystem owner | DB forced 0600; state directory 0700; include private WAL/SHM | SQLite online backup or quiesced DB+WAL checkpoint, never a blind live-file copy | durable until explicit deprecation/archive policy; candidate expiry remains schema policy | stop writers or use SQLite backup API, restore, run integrity/schema checks, then smoke recall | migrate DB with schema migrations and WAL consistency; never substitute an unproven DB path |
| unified config | `EDUFLOW_CONFIG_FILE` or `./eduflow.toml`; legacy fallback `EDUFLOW_TEAM_FILE`/`./team.json` and `EDUFLOW_RUNTIME_CONFIG`/`./runtime_config.json` | operator/config deployment | tunables, team/runtime registry, lifecycle and daemons | runtime operator | repository/deployment ACL; config must contain no secret values | git/deployment-config backup | version history for supported releases | restore approved TOML, validate parse and audit the live process config path/hash | migrate legacy JSON sections into TOML, then remove ambiguity only after all launchers use TOML |
| tenant token cache | `$STATE/.tenant_token.json` | `eduflow.feishu.lark._ensure_tenant_token` | Feishu/lark subprocess bootstrap | runtime operator | created 0600; parent 0700; contains a short-lived token | do not back up; recreate | until `expire_at`; refresh before expiry | delete and refetch from an authorized credential source | never copy between hosts/deployments; recreate after state migration |
| agent secret source | `EDUFLOW_SECRETS_FILE` or `$STATE/.env`; per-agent login credential under `$STATE/agent-home/<agent>/` when selected | operator/provisioning and CLI login | `src/eduflow/runtime/agent_auth.py` at spawn | credential owner/runtime operator | secrets file and login material 0600; directories 0700; gitignored | approved encrypted secret manager only, not ordinary state backup | rotate to provider policy and on role/personnel change | restore from authorized secret manager or re-authenticate, then test resolution mode without printing values | re-provision, never copy into `eduflow.toml`; preserve token > login > API-key selection intentionally |
| scheduled task store | `$STATE/scheduler/{meta.json,rules.json,occurrences.json,lanes.json,notifications.jsonl,heartbeat.json,cursor.json}` | scheduler engine and `eduflow.store.scheduled_tasks` | scheduler, manager panels, health and notifications | scheduler/manager control plane | private state tree; locked atomic JSON plus append-only notifications | consistent snapshot of the entire scheduler directory | cancelled rules remain audit records; notifications follow operational audit window | restore directory as one checkpoint, validate D-ID counter, versions, cursor and heartbeat before enabling ticks | migrate independently from T-ID tasks; preserve D-IDs, CAS versions and occurrence/lane links |

## Production-source proof and safety checks

- `src/eduflow/runtime/paths.py` resolves `$STATE`, `eduflow_memory.db`, task events, runtime status, scheduler files, and the unified config path.
- `src/eduflow/memory/db.py` opens that exact memory DB with SQLite WAL/busy timeout and applies mode 0600. An unknown memory DB location is a deployment failure, not a permissible inventory value.
- `src/eduflow/runtime/agent_auth.py` proves the agent credential source and resolution priority without requiring disclosure of secret values. An unknown credential source must block launch/migration rather than be guessed.
- `src/eduflow/feishu/lark.py` creates `$STATE/.tenant_token.json` with mode 0600 and otherwise delegates host authentication to lark-cli. Never print or commit token, key, password, secret, or credential contents while checking these paths.

Before backup, restore, or migration, capture the live process `EDUFLOW_STATE_DIR` and `EDUFLOW_CONFIG_FILE`, resolve both paths, and compare them with the wrapper target. A directory existing on disk is not proof that production reads it.
